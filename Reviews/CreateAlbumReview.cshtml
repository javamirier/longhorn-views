@model LonghornMusic.Models.AlbumReview

@{
    ViewBag.Title = "CreateAlbumReview";
}

<h2>Review Album</h2>

@using (Html.BeginForm())
{
    <p class="form-group">
        Album: @ViewBag.AlbumName by @ViewBag.ArtistName <br />

        <label class="form-check-label">
            Select a Rating: 1 @Html.RadioButton("AlbumScore", 1, true, new { @class = "form-check-input" })

            2 @Html.RadioButton("AlbumScore", 2, new { @class = "form-check-input" })

            3 @Html.RadioButton("AlbumScore", 3, new { @class = "form-check-input" })

            4 @Html.RadioButton("AlbumScore", 4, new { @class = "form-check-input" })

            5 @Html.RadioButton("AlbumScore", 5, new { @class = "form-check-input" })
        </label> <br />
        <label class="form-check-label">
            Comment (optional) @Html.TextBox("AlbumReviewText", null, new { @class = "form-control" }) <br />
        </label>
    </p>

    @Html.AntiForgeryToken()
    <div class="form-horizontal">
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Create" class="btn btn-default" />
            </div>
        </div>
    </div>
}









